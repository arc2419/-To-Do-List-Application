# -To-Do-List-Application
# To-Do List CLI App

A simple console-based to-do list application built using Python.

## Features
- Add tasks
- View tasks
- Remove tasks
- Saves tasks in a text file so they remain after restarting the program

## How to run
```bash
python ToDo.py
